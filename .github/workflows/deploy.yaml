name: Shuttle Deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: AliSajid/shuttle-deploy-action@b9a322d
        with:
          deploy_key: ${{ secrets.SHUTTLE_DEPLOY_KEY }}